<nav>
  <h1>Dev News</h1>

    <div class="nav-right">
      <% if user_signed_in? %>
        <button class="navbar-hamburger" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-bars"></i>
       </button>

        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
          <%= link_to "Log out", destroy_user_session_path, method: :delete,
          class: "dropdown-item" %>
        </div>

      <% else %>
        <%= link_to "Login", new_user_session_path, class: "nav-link", id: "login" %>
      <% end %>

    </div>
</nav>




